import 'package:flutter/material.dart';

class PackageDeliveryAppBar extends AppBar {
  PackageDeliveryAppBar({super.key})
      : super(
          title: const Text("PackageDelivery App"),
        );
}
