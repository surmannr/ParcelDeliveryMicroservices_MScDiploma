import { DaysFromIntArrayPipePipe } from './days-from-int-array.pipe.pipe';

describe('DaysFromIntArrayPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DaysFromIntArrayPipePipe();
    expect(pipe).toBeTruthy();
  });
});
