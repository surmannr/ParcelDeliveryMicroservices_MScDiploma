import { DaysFromIntArrayPipe } from './days-from-int-array.pipe';

describe('DaysFromIntArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysFromIntArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
